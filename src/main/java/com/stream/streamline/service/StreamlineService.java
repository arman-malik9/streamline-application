package com.stream.streamline.service;

import org.springframework.stereotype.Service;

import com.stream.streamline.model.SignUpModel;


public interface StreamlineService {
	
	void addUser(SignUpModel u);

}
